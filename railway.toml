[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "/bin/bash -c 'cd /app && /usr/local/bin/init_db.sh && php -S 0.0.0.0:8000 router.php'"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
